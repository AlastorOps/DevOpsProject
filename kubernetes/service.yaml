apiVersion: v1
kind: Service
metadata:
  name: ems-ops-frontend-svc
  namespace: ems-ops
  labels:
    app: ems-ops-frontend
spec:
  selector:
    app: ems-ops-frontend
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
