{
  "name": "ems-ops",
  "private": true,
  "scripts": {
    "dev": "concurrently --names BACK,FRONT --prefix-colors cyan.bold,magenta.bold \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .venv\\Scripts\\uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
